import { useState } from 'react'
export default function UserForm(){ const [users,setUsers]=useState([]); const [u,setU]=useState(''); const [p,setP]=useState(''); const add=e=>{e.preventDefault(); setUsers([...users,{u,p}]); setU(''); setP('');}; const del=i=>setUsers(users.filter((_,x)=>x!==i)); return (<div><form onSubmit={add}><input value={u} onChange={e=>setU(e.target.value)} placeholder='Username'/><input type='password' value={p} onChange={e=>setP(e.target.value)} placeholder='Password'/><button type='submit'>Add</button></form>{users.map((x,i)=><div key={i}>{x.u} — {x.p}<button onClick={()=>del(i)}>Delete</button></div>)}</div>)}